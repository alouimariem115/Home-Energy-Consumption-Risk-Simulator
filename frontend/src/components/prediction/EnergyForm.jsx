// NOTE:
// This component was an early standalone demo for calling the /predict
// endpoint directly. The main dashboard now uses PredictionForm +
// PredictionResult + predictionService, so this file is kept only as a
// reference example and is not used in the live UI.

export default function EnergyForm() {
  return null;
}
